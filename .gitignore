.DS_Store
.claude/

# Book PDF chunks (large files, not tracked)
books/

# Development source directories (deployed builds are in ba/ and aa/)
basic-algebra/
abstract-algebra/

# Node modules (all courses and shared)
node_modules/
*/node_modules/
shared/node_modules/

# Build outputs (kept separate from deployed assets)
*/dist/

# Firebase
firebase/functions/node_modules/
firebase/functions/lib/
firebase/*.log
firebase/functions/*.log
.vercel

# Environment variables
.env
.env.local
.env.*.local

# Firebase config (contains API keys)
scripts/firebase-config.js

# Working files
calc_easy_pdfs/
why_math_chunks/
