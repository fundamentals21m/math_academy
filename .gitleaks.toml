# Gitleaks configuration
# Firebase client-side API keys are intentionally public in built assets
# They are protected by Firebase security rules, not key secrecy

[allowlist]
description = "Allow Firebase client-side API keys in deploy folders"

# Allow Firebase API keys in bundled JS files (deploy folders)
paths = [
    '''.*-deploy/assets/.*\.js$''',
]
