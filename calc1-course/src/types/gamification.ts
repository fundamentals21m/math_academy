/**
 * Gamification types - Re-exported from shared package
 */
export type {
  GamificationState,
  SectionProgress,
  AchievementProgress,
  MasteryLevel,
  StreakData,
  AchievementNotification,
  AchievementCategory,
  Difficulty,
  SectionId,
} from '@shared/gamification/types';

// Alias for backward compatibility
export type { Difficulty as QuizDifficulty } from '@shared/gamification/types';
