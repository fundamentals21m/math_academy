/**
 * PostCSS configuration for linalg course.
 * Uses Tailwind CSS v4 which requires @tailwindcss/postcss plugin.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
